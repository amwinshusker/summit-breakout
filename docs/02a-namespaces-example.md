# Namespace example

```bash
# Create three deployments and two namespaces
# the first namespace will have two deployments inside
kubectl apply -f ./docs/ops-training/ops-training-manifests/namespaces/namespaces.yaml
```
